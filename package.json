{
  "name": "e-gov-hourei-to-obsidian",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "pnpm build && node --test dist/**/*.test.js",
    "test:e2e:docker": "bash scripts/e2e_docker.sh",
    "poc": "node scripts/poc_fetch_law.mjs"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3"
  }
}
